# Icons

UI icons are inlined as SVG strings inside `js/ui.js` (see `ICONS`) so the
app needs zero network requests and zero external icon fonts/libraries.

This folder is reserved for future custom icon assets (e.g. a dedicated
app icon set for the PWA manifest at multiple resolutions).
